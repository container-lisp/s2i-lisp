#!/bin/bash

SBCL_OPTS=(
    "${SBCL_OPTS[@]}"
    --dynamic-space-size "${APP_MEM-256}"
    --eval "(ql:quickload :$(eval echo ${APP_SYSTEM_NAME}))"
    --eval "$(eval echo ${APP_EVAL})"
)

sbcl "${SBCL_OPTS[@]}"
